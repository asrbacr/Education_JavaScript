// этот файл будет в приоритете, а не jsx если не указывать расширение при import
function Greeting() {
    return ( 
        <div className="App">
            <h2>Привет, React!</h2>
        </div>
     );
}

export default Greeting;