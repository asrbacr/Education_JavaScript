/**
 * Функция число в квадрате
 * @param {number} x - Число для произведения математического действия
 * @returns Возвращает число в квадрате
 */
function square(x) {
  return x * x;
}

module.exports = { square };
